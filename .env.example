# Copy to .env.local and fill in values from your Supabase project
# (Project Settings → API).
# If both are unset, the app falls back to a localStorage + BroadcastChannel
# shim that only syncs across tabs on the same machine.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
